//! Expression variants for symbolic algebra

use crate::core::types::Exp;

/// Generate variants of an expression for comparison purposes
pub fn variants(_exp: &Exp) -> Vec<Exp> {
    // Simplified - return the expression itself
    vec![]
}
